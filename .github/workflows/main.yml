name: btu-sks

on:
  workflow_dispatch:

jobs:
  build:
    runs-on:
      - self-hosted
      - label1
    steps:
      - uses: actions/checkout/@v2
      - name: btu-sks
        run: |
          echo hello,world
          sleep 1
      
